% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{precM.NL.one}
\alias{precM.NL.one}
\title{Estimate one row of precision matrix via Nodewise Lasso}
\usage{
precM.NL.one(
  X,
  row.idx,
  method = "EBIC",
  gamma = 0.5,
  n.lambda = 100,
  lambda = NULL
)
}
\arguments{
\item{X}{A size \eqn{n \times p} matrix of predictors (e.g., genetic variants). Missing values are not allowed}

\item{row.idx}{An integer from 1 to \eqn{p} indicating which row of the precision matrix to estimate.}

\item{method}{A character, either \code{"EBIC"} or \code{"CV"}, determining how \code{lambda} is chosen}

\item{gamma}{A numeric value in \([0, 1]\) for the EBIC hyperparameter. When \code{gamma = 0}, EBIC reduces to the ordinary BIC. Default is \code{0.5}}

\item{n.lambda}{An integer giving the number of \code{lambda} points for \code{glmnet}. Default is \code{100}}

\item{lambda}{A nonnegative numeric vector (optional user-supplied \code{lambda} grid). Default is \code{NULL}}
}
\value{
A list with the following elements:
\item{beta}{A length-\eqn{(p-1)} vector of coefficients for the selected row of the precision matrix}
\item{tau.sq}{A numeric value representing the residual variance plus penalty contribution}
}
\description{
Estimates one row of the precision matrix via Nodewise Lasso, serving as a helper function
for \code{precM.NL.all} to facilitate parallelization.
}
