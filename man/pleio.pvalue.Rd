% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{pleio.pvalue}
\alias{pleio.pvalue}
\title{Compute group p-values for DrFARM}
\usage{
pleio.pvalue(X, Y, Theta, B, E.Z, precM, standardize = TRUE)
}
\arguments{
\item{X}{A size \eqn{n \times p} matrix of predictors (e.g., genetic variants). Missing values are not allowed}

\item{Y}{A size \eqn{n \times q} matrix of outcomes (e.g., continuous omics traits). Missing values are not allowed}

\item{Theta}{A size \eqn{q \times p} initial coefficient matrix}

\item{B}{A size \eqn{q \times k} factor loading matrix}

\item{E.Z}{A size \eqn{n \times k} latent factor score matrix}

\item{precM}{A size \eqn{p \times p} precision matrix}

\item{standardize}{A logical indicating whether to standardize \code{X} and \code{Y} by column. Default is \code{TRUE}}
}
\value{
A length-\eqn{p} vector of p-values, where each p-value corresponds to one predictor across all outcomes
}
\description{
Computes the historical two-sided Cauchy-tail combination, one value per predictor.
}
\details{
The formula is T = mean(cot(pi * p)) across entry p-values, followed
by 2 * pcauchy(-abs(T)). This is retained for compatibility; it is not the
conventional one-sided Cauchy combination. In particular, equal p-values
approaching either zero or one can yield small combined values. Boundary
underflow/cancellation and calibration are unresolved. No generalized-response
inference or kinship-adjusted inference is provided by this function.
}
